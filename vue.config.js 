module.exports = {
    publicPath: '/',
    configureWebpack: {
        module: {
            rules: [{
                test: /\.json$/,
                loader: 'json', // yarn add json-loader
            }]
        }
    }
}